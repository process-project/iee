= render('profiles/layout') do
  = simple_form_for(current_user, url: profile_compute_site_proxy_path, html: { method: :put }) do |f|
    = f.input :lol
    .form-group
      .col-md-6.col-sm-6.col-xs-12.col-md-offset-3
        = f.button :submit, 'submit', class: 'btn btn-success'