= form_tag(patient_comparison_path(patient), method: :get) do
  %table.table
    %thead
      %tr
        %th.iid= t('.id')
        %th.select
        %th= t('.name')
        %th= t('.user')
        %th= t('.status')
    %tbody
      = render partial: 'patients/pipelines/pipeline_compare',
               collection: pipelines, as: :pipeline

  = submit_tag(t('.compare'), class: 'btn btn-success')
