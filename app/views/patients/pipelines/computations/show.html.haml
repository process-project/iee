= render 'patients/pipelines/show', patient: @patient, pipeline: @pipeline, details: @details do
  = render 'patients/pipelines/computations/show',
    patient: @patient, pipeline: @pipeline,
    computation: @computation, computations: @computations
