= simple_form_for [patient, pipeline] do |f|
  = f.error_notification
  = f.input :name
  = f.input :mode, readonly: !pipeline.new_record?, collection: Pipeline.modes.keys
  .ln_solid
  .form-group
    .col-md-6.col-md-offset-3
      = f.button :submit, class: 'btn btn-success'
