= render 'patients/show', patient: @patient, details: @details do
  = render 'patients/all_pipelines_outputs', patient: @patient

.x_panel
  .x_title
    %h2
      = t 'patients.pipelines.new.title'

    %ul.nav.navbar-right.panel_toolbox
      %li
        = link_to patient_path(@patient), class: 'back' do
          = icon 'hand-o-left',
            title: t('patients.pipelines.new.back_to_index')
    .clearfix

  .x_content
    = render 'patients/pipelines/form',
              patient: @patient, pipeline: @pipeline
