en:
  steps:
    segmentation:
      cannot_start: |
        Cannot start segmentation because required input file
        (imaging_*.zip) cannot be found in the project input directory or
        pipeline directory.
      created: |
        All required input files are available.
        You can start segmentation for this project.
    parameter_extraction:
      cannot_start: |
        Cannot start parameter extraction because the required input files
        (OFF mesh) cannot be found in the project input directory or pipeline directory.
      created: |
        All required input files are available.
        You can start parameter extraction for this project.
    cfd:
      cannot_start: |
        Cannot start CFD simulation because the required input files
        (*Trunc.off) cannot be found in the project input directory or pipeline directory.
      created: |
        All required input files are available.
        You can start CFD simulation for this project.
    rom:
      cannot_start: |
        Cannot start ROM analysis because the required input files
        (*.dxrom) cannot be found in the project input directory or pipeline directory.
      created: |
        All required input files are available.
        You can start ROM analysis for this project.
    parameter_optimization:
      cannot_start: |
        Cannot start parameter optimization because the required input files
        (ValveChar.csv) cannot be found in the project input directory or pipeline directory.
      created: |
        All required input files are available.
        You can start parameter optimization for this project.
    uncertainty_analysis:
      created: |
        All required input files are available.
        You can start uncertainty analysis for this project.
    blood_flow_simulation:
      cannot_start: |
        Cannot start blood flow simulation because required input files
        (fluidFlow.cas and structural_vent.dat) cannot be found
        in the project input directory or pipeline directory.
      created: |
        All required input files are available.
        You can start blood flow simulation for this project.
    heart_model_calculation:
      title: Heart Model Calculation
      cannot_start: |
        Cannot start heart model calculation because required input file
        (0DModel_input.csv) cannot be found in the project input directory or
        pipeline directory.
      created: |
        All required input files are available.
        You can start heart model calculation for this project.
      error: There were an error while calculating project.
    0d_models:
      cannot_start: |
        Cannot run 0D models because required input files
        (OutFileGA.csv) cannot be found in the project input directory or
        pipeline directory.
      created: |
        All required input files are available.
        You can start 0D model sequence for this project.
    pressure_volume_display:
      cannot_start: |
        Cannot start PV visualization because required input files
        (OutSeries1.csv, OutSeries2.csv, OutSeries3.csv, OutSeries4.csv)
        cannot be found in the project input directory or pipeline directory.
      created: |
        All required input files are available.
        You can start PV visualization for this project.

    # Mocked steps

    patient_db_selection:
      title: Project database selection